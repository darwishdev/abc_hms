from setuptools import setup, find_packages

setup(
    name="abc_hms",
    version="0.0.1",
    description="Custom Hotel Management App for Frappe",
    author="Darwish",
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
    install_requires=[],
)

